import React from 'react';

const Header = () => {
	return (
		<div style={{ padding: '4vh 0 0 5vw' }}>
			<span style={{ fontSize: 'calc(20px + 3vw)', color: 'red' }}>Wild</span>
			<span style={{ fontSize: 'calc(20px + 3vw)', color: 'orange' }}>
				Fires
			</span>
		</div>
	);
};

export default Header;
